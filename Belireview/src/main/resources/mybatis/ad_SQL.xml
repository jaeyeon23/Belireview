<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="ad">

<insert>
insert into AD(	
	AD_NO,
	AD_NAME,
	AD_LINK,
	AD_CONTENT,
	AD_COMPANY,
	AD_READCOUNT,
	AD_POSTER_IMAGE,
	AD_MAIN_IMAGE,
	AD_CONTENT_IMAGE,
	AD_GRADE,
)
values (
	SEQ_AD_NO.NEXTVAL,
	#{AD_NAME},
	#{AD_LINK},
	#{AD_CONTENT},
	#{AD_COMPANY},
	#{AD_READCOUNT},
	#{AD_POSTER_IMAGE},
	#{AD_MAIN_IMAGE},
	#{AD_CONTENT_IMAGE},
	#{AD_GRADE},
)
</insert>

<select>
SELECT
	RNUM FROM 
	AD_NO,
	AD_NAME,
	AD_LINK,
	AD_CONTENT,
	AD_COMPANY,
	AD_READCOUNT,
	AD_POSTER_IMAGE,
	AD_MAIN_IMAGE,
	AD_CONTENT_IMAGE,
	AD_GRADE,
	
  (SELECT
	ROWNUM RNUM,
	AD_NO,
	AD_NAME,
	AD_LINK,
	AD_CONTENT,
	AD_COMPANY,
	AD_READCOUNT,
	AD_POSTER_IMAGE,
	AD_MAIN_IMAGE,
	AD_CONTENT_IMAGE,
	AD_GRADE FROM 
    (SELECT * FROM AD ORDER BY AD_NO DESC) ORDER BY RNUM ASC) WHERE RNUM BETWEEN #{START} AND #{END}
</select>

<select>
SELECT
	AD_NO,
	AD_NAME,
	AD_LINK,
	AD_CONTENT,
	AD_COMPANY,
	AD_READCOUNT,
	AD_POSTER_IMAGE,
	AD_MAIN_IMAGE,
	AD_CONTENT_IMAGE,
	AD_GRADE FROM AD WHERE AD_NO = #{AD_NO}
</select>

</mapper>