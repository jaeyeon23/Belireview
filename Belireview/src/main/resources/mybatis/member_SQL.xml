<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<!-- 로그인 -->
	<select id="selectOneMember" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT * FROM MEMBER WHERE ID=#{ID}
		]]>
	</select>
	
	<!-- 아이디찾기 -->
	<select id="searchId" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT ID FROM MEMBER
			WHERE NAME=#{NAME} AND EMAIL=#{EMAIL}
		]]>
	</select>
	
	<!-- 비밀번호찾기 -->
	<select id="searchPw" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT PASSWORD FROM MEMEBER
			WHERE ID=#{ID} AND NAME=#{NAME} AND EMAIL=#{EMAIL}
		]]>
	</select>
	
</mapper>
   <!--    <insert id="insertMember" parameterType="hashmap">
		<![CDATA[
		INSERT INTO MEMBER ( MEMBER_NO, ID, PASSWORD, NAME, EMAIL, TEL, ADMIN, REGDATE, PROFILE_IMAGE, EMAIL_MARKETING, SMS_MARKETING) 
		VALUES( MEMBER_NO_SEQ.nextVal, #{ID}, #{PASSWORD}, #{NAME},
		 #{EMAIL}, #{TEL}, #{ADMIN}, 
		 #{REGDATE}, #{PROFILE_IMAGE}, #{EMAIL_MARKETING}, #{SMS_MARKETING})
		]]>
	  </insert>
	

	   ID 찾기 정보 가져오기 확인
      
      <select id="Idcheck" parameterType="hashmap" resultType="int">
  		 <![CDATA[
   		   SELECT COUNT(*) FROM MEMBER WHERE ID = #{ID}
   		]]>
   	  </select>
   -->
